<%- include('../partial/head',{title:"Detalle"}) %>
<body>
    <%- include('../partial/header') %>
    <main class="login--main">
        <form id="form__login" action="/users/login" method="POST">
            <h1><i class="fa-solid fa-user"></i> Login</h1>

            <label for="email">Email</label>
            <input type="email" name="correo" id="email" required value="<%= locals.correo ? correo : ""%>" placeholder="  ">
            
            <label for="password">Contraseña</label>
            <input type="password" name="contrasena" id="password" value="<%= locals.contrasena ? contrasena : ""%>" required minlength="6" placeholder="  ">
            
            <% if(locals.errores) {%>
                <p> <i class="fa-solid fa-circle-exclamation"></i> Credenciales invalidas</p>
            <% } %>
            <div class="redordarme">
                <label for="recuerdame">Desea que recordemos su sesion?</label>
                <input type="checkbox" name="recuerdame" id="recuerdame">
            </div>
                <button type="submit">Login</button>
        </form>
    </main>
    <footer>
    </footer>
</body>